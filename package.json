{
  "name": "tres",
  "version": "0.0.2",
  "description": "Exercise three",
  "repository": "https://github.com/javivi_113/DIW",
  "author": "javivi113 <javiasterio@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "parcel build *.html",
    "prebuild": "test -d dist || mkdir dist  && cp -rv images dist ",
    "prestart": "yarn prebuild",
    "start": "parcel serve *.html --open"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.0.0",
    "autoprefixer": "^10.3.7",
    "parcel": "^2.0.0",
    "posthtml-extend": "^0.6.0",
    "posthtml-include": "^1.7.2"
  }
}
